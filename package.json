{
  "name": "httpulse",
  "version": "1.0.0",
  "description": "⚡ Monitor de Saúde HTTP & Sonda de API — Monitore a saúde de suas APIs com visualizações em tempo real no terminal",
  "type": "module",
  "bin": {
    "httpulse": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup && printf '#!/usr/bin/env node\\n' | cat - dist/cli.js > dist/cli.tmp && mv dist/cli.tmp dist/cli.js && chmod +x dist/cli.js",
    "dev": "tsup --watch",
    "start": "node dist/cli.js"
  },
  "keywords": [
    "http",
    "health-check",
    "monitoring",
    "api",
    "probe",
    "cli",
    "terminal",
    "ssl",
    "dns",
    "latency",
    "uptime"
  ],
  "author": "nesquiksuquinho",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "cli-table3": "^0.6.5",
    "commander": "^13.1.0",
    "undici": "^7.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.8",
    "tsup": "^8.4.0",
    "typescript": "^5.8.2"
  }
}